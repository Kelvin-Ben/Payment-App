<p style="color: green"><%= notice %></p>

<h1>Records</h1>

<div id="records">
  <% @records.each do |record| %>
    <%= render record %>
    <p>
      # <%= link_to "Show this record", category_records_url %>
    </p>
  <% end %>
</div>

<%= link_to "New record", new_category_record_path %>